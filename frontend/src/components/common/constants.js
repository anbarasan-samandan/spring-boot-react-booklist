/*All the constants goes here*/

/* The number of total stars to be shown in the star rating component */
export const STAR_RATING = 5;

/* The limit of the records to be fetched from the server in a single request */
export const PAGE_SIZE = 10;

/*
 * Style for setting the font size of the price...
 */
export const priceStyle = {
  fontSize: "1em",
};

/*
 * Style for setting the image to a fixed size, bigger than in the BookList component...
 */
export const imageStyle = {
  width: "150px",
  height: "175px",
};

/*
 * Style for positoning the image to the left.
 */
export const leftDiv = {
  float: "left",
  margin: "2 2 2 2",
};

/*
 * Style for positioning the book details to the right.
 */
export const rightDiv = {
  float: "right",
};

/*
 * Style for making the images to be rendered in fixed width.
 */
export const tableListimageStyle = {
  width: "80px",
  height: "80px",
};

/*
 * Style for setting the background color of the table hader.
 */
export const tableHeaderStyle = { backgroundColor: "#ffc107" };

/*
 * Style for active link (the menus on the top of the page)
 */
export const activeStyle = { color: "#f15B2A" };

/*
 * Setting the font style of the menus...
 */
export const menuFontSizeStyle = {
  fontSize: "24px",
  color: "#FFC107",
  fontSizeStyle: "bold",
};

/*
 * Setting for the pipe style shown between the menus as a separator
 */
export const menuPipeStyle = {
  fontSize: "24px",
  fontSizeStyle: "bold",
  color: "#FFBF00",
};
